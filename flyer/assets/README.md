# Flyer assets

- `staff-photo-main.jpg`: 表面右上の店舗スタッフ写真。推奨サイズ 960 x 1400px。配置するときは `front.html` の `.photo-placeholder` を画像要素へ差し替えます。
- `qr-code.png`: `export.mjs` が `https://daida-store.jp` から自動生成します。Git管理対象外です。

実写真が未提供のため、現在は写真エリアをグレーのプレースホルダーで表示します。
